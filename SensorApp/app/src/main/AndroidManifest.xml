<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sensor.com.sensorapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailsActivity"/>
        <activity android:name=".SensorListActivity"
            android:label="@string/resources"/>
        <activity android:name=".PayActivity"
            android:label="@string/pay_methods"/>
        <activity android:name=".ChartDisplayActivity"
            android:label="@string/sensor_data"/>
        <activity android:name=".DataOptionsActivity"
            android:label="@string/sensor_data"/>
    </application>

</manifest>
